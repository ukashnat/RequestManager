/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.requestmanager.controller;

import com.requestmanager.model.EnumStatus;
import org.junit.Assert;
import org.junit.Test;

/**
 *
 * @author pldh0199
 */
public class RequestHandlerTest {

    final static private String fileName = "requests.xml";

 /*   @Test
    public void TestCreateNewRequest() {
        RequestHandler tester = new RequestHandler(null);
        tester.createNewRequest(null, null);
        Assert.assertEquals("", "");
    }
    
    @Test
    public void TestCreateRequest() {
        RequestHandler tester = new RequestHandler(null);
        tester.acceptRequest(null,EnumStatus.Status.VERIFIED);
        Assert.assertEquals("", "");
        tester.acceptRequest("",EnumStatus.Status.VERIFIED);
        Assert.assertEquals("", "");
    }*/
}
